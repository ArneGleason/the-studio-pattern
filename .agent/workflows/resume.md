---
description: Resume work on the canonical Studio Pattern repository
---

1. Read `AGENTS.md`.
2. Read `README.md` and `LOCAL_DEV_NOTES.md`.
3. Read `.agent/session.json`.
4. Read the latest handoff under `.agent/handoffs/` if one is listed.
5. Check current git state.
   ```sh
   git status --short
   ```
6. Continue from the newest user request, not from stale handoff assumptions.
